[error_codes]

[messages]
header = The Squirrel system understands the following env variables (all optional):\n\nValues in YELLOW are the currently set value for that variable.\nIf a variable has not been set in the current shell, that will be noted (variables are strictly optional).\n
not_set = {{COLOR_BRIGHT_RED}}This variable has not been set in this shell.{{COLOR_NONE}}
equals = ={{COLOR_BRIGHT_YELLOW}}
no_color = {{COLOR_NONE}}

[description]
Displays a full list of all squirrel env variables and any values they have (if they have been set in the current shell).

[usage]
%(prog)s [options]

[options-language]
short_flag = -l
long_flag = --language
action = store
dest = language
default = english
type = str
metavar = language
nargs = 1
required = False
description = Use this language. If none given, or if the language is not installed, defaults to 'english'.
